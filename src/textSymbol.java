import jdk.jfr.Experimental;

/*@// TODO: 24/08/2023 is this the right way to use interface? */
@Experimental
public interface textSymbol {
     char space = ' ', floor = '_', wall = '#',player = 'x', monster = 'm';
}
